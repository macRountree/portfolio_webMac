---
import  Section  from '../Section.astro'
import {skills} from '@/resume.json'
console.log(skills)
---
<Section title="Skills">

    <ul>
        {skills.map(({name}) => (
            <li>
                <span>{name}</span>
            </li>
        ))}
    </ul>
</Section>

<style>
    ul{
        display: inline-flex  ;
        gap: 1rem;
        flex-wrap: wrap;
    }
    li{
        border-radius:10px;
        background: #444 ;
        color: #eeeeeef5;
        font-size: 1rem;
        font-weight: 500   ;
        padding: 0.2rem .6rem ;
    }
</style>