{
  "extends": "astro/tsconfigs/strict",
  //*Podemos agregar configuraciones adicionales aquí como poder usar el @ para importar archivos
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@resume": ["./resume.json"],

      "@/*": ["src/*"]
    }
  }
}
